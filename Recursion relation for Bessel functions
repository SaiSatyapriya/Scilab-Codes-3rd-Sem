clc;
funcprot(0);

n = 1;

for x = 0:0.01:10

    p = x * besselj(n+1, x);

    q = 2*n*besselj(n, x) - x*besselj(n-1, x);

    if p == q then

    elseif abs(p - q) < 0.000001 then

    else
        disp("The recursion relation fails by " + string(p - q));
    end
end
